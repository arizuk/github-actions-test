name: ci-test

on: push

jobs:
  ci_test:
    name: ci test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Hello world
        run: echo hello world
